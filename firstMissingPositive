import java.util.Arrays;
class Solution {
    public int firstMissingPositive(int[] nums) {
        Arrays.sort(nums);
        int least =1;
        for(int num:nums){
            if(num==least){
                least++;
            }
        }
        return least;
    }
}
