class Solution {
    public int mirrorDistance(int n) {
        return Math.abs(reverse(n)-n);
    }
    int reverse(int n){
        int reverse = 0;
        while(n!=0){
            int remainder = n%10;
            n/=10;
            reverse = reverse*10 + remainder;
        }
        return reverse;
    }
}
