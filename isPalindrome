class Solution {
    public boolean isPalindrome(int x) {
        int res = x;
        int rev_num = 0;
        int remainder = 0;
        while (x>0){
            remainder = x%10;
            rev_num = (rev_num*10) + remainder;
            x = x/10;
        }
        return rev_num==res;
    }
}
