class Solution {
    public int uniquePaths(int m, int n) {
        long ans =1;
        int totalMove = m+n-2;
        int choice = Math.min(m-1,n-1);
        for(int i=1;i<=choice;i++){
            ans = ans*(totalMove-choice+i)/i;
        }
        return (int) ans;
    }
}
